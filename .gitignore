# .NET
bin/
obj/
*.user
*.suo
[Tt]est[Rr]esults/

# Node
node_modules/
dist/
.svelte-kit/
frontend/build/
frontend/src/lib/api/schema.d.ts

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Local operational state (never committed; lives on a volume in deployment)
*.db
*.db-wal
*.db-shm

# Local development: the scratch wiki repository and state database `make dev-setup` creates,
# and the hub environment `make run` reads (it holds GRIMOIRE_MODEL_TOKEN).
.local/
.env

# Personal compose overrides (e.g. a bind-mounted host wiki path) — never generic across machines.
deploy/compose.override.yaml

# Editors and OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
