/**
 * The build's entry point, so the compiled runner is at `src/agentrun/dist/main.js` — the exact
 * path `src/dispatch/adapters/RunnerProcess.cs` spawns (contracts/runner-protocol.md "Invocation").
 */

import "./run/main.js";
