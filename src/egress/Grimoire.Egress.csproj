<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>Grimoire.Egress</RootNamespace>
  </PropertyGroup>

  <!-- Yarp.ReverseProxy lives here and nowhere else (plan V.3, architecture gate 2). -->
  <ItemGroup>
    <PackageReference Include="Yarp.ReverseProxy" />
  </ItemGroup>

</Project>
